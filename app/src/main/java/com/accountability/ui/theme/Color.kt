package com.accountability.ui.theme

import androidx.compose.ui.graphics.Color

// ─── Wabi-sabi Zen Palette ────────────────────────────

// Primary: Matcha Green — earthy, grounding
val MatchaGreen = Color(0xFF4A6741)
val MatchaGreenLight = Color(0xFF6E8E63)
val MatchaGreenDark = Color(0xFF2F4A28)

// Secondary: Sakura Blush — soft feminine accent
val SakuraBlush = Color(0xFFE8B4B8)
val SakuraBlushDark = Color(0xFFC48E92)

// Tertiary: Aged Copper — warm warning / tertiary accent
val AgedCopper = Color(0xFFB5541A)
val AgedCopperLight = Color(0xFFD4804E)

// Backgrounds
val WashiPaper = Color(0xFFF5F0E8)       // Light mode background — rice paper
val DeepInk = Color(0xFF0D0D10)          // Dark mode background — sumi night
val WashiPaperDark = Color(0xFFEDE7DB)   // Slightly darker washi for surfaces

// Surface variants
val SurfaceLight = Color(0xFFF0EBE1)     // Card surfaces, light mode
val SurfaceDark = Color(0xFF1A1A1F)      // Card surfaces, dark mode
val SurfaceDarkVariant = Color(0xFF252529)

// Text / Ink
val SumiInk = Color(0xFF1C1C1E)          // Primary text — dark mode on-surfaces
val SumiInkSoft = Color(0xFF3A3A3C)      // Secondary text — lighter ink
val WashiText = Color(0xFFE8E2D6)        // On-dark text — warm white, not harsh
val WashiTextDim = Color(0xFFA09A8E)     // Subtle labels on dark

// Error
val ErrorRust = Color(0xFFB7410E)        // Rust red — earthy error
val ErrorRustLight = Color(0xFFD4673B)

// Divider / Border
val HairlineBorder = Color(0x1A1C1C1E)   // Very subtle ink border (10% opacity)
val HairlineBorderDark = Color(0x1AE8E2D6)
